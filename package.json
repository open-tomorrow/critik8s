{
  "name": "critik8s",
  "version": "1.0.0",
  "description": "monitoring tool for k8s",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:md": "markdownlint-cli2 --fix **/*.md",
    "generate:models": "echo generate models and API interface",
    "cluster:create": "k3d cluster create critik8s -p '80:80@loadbalancer'",
    "cluster:delete": "k3d cluster delete critik8s",
    "critik8s:create:ns": "kubectl create ns critik8s",
    "rabbitmq:install": "helm install rabbitmq-operator charts/rabbitmq-operator -n critik8s && helm install rabbitmq-cluster charts/rabbitmq-cluster --values settings.yaml -n critik8s",
    "rabbitmq:uninstall": "helm uninstall rabbitmq-cluster -n critik8s && helm uninstall rabbitmq-operator -n critik8s",
    "monitor-backend:install": "helm install monitor-backend charts/monitor-backend/ --values settings.yaml -n critik8s",
    "monitor-backend:uninstall": "helm uninstall monitor-backend -n critik8s",
    "data-collector:install": "helm install data-collector charts/data-collector/ --values settings.yaml -n critik8s",
    "data-collector:uninstall": "helm uninstall data-collector -n critik8s",
    "amqp-proxy:install": "helm install amqp-proxy charts/amqp-proxy/ --values settings.yaml -n critik8s",
    "amqp-proxy:uninstall": "helm uninstall amqp-proxy -n critik8s",
    "critik8s:install": "npm run monitor-backend:install && npm run data-collector:install && npm run amqp-proxy:install",
    "critik8s:get:all": "kubectl get all -n critik8s"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/open-tomorrow/critik8s.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/open-tomorrow/critik8s/issues"
  },
  "homepage": "https://github.com/open-tomorrow/critik8s#readme",
  "devDependencies": {
    "markdownlint-cli2": "^0.10.0"
  }
}
